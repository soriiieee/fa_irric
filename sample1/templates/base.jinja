<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Page Title</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" media="screen" href="main.css" />
    <script src="main.js"></script>
</head>
<body>

    <ul class = "nav">
        <li><a href="{{url_for('app.home')}}">ホーム</a></li>
        {% if current_user.is_authenticated %}
        <li><a href="{{url_for('app.logout')}}">ログアウト</a></li>
        {% else %}
        <li><a href="{{url_for('app.login')}}">ログイン</a></li>
        <li><a href="{{url_for('app.register')}}">登録</a></li>
        {% endif %}
        <li><a href="{url_for{'app.user'}}">ユーザ画面</a></li>
    </ul>

    {# layoutのフォーマット画面を表示する #}
    {% block content%}
    {% endblock%}
</body>
</html>